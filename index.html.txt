<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Jesse & Co</title>

<style>
    body {
        margin: 0;
        font-family: Arial, sans-serif;
        background: #0b0f1a;
        color: white;
    }

    /* HEADER / BANNER (like your original vibe) */
    header {
        background: linear-gradient(90deg, #00f5ff, #7c3aed);
        padding: 18px;
        text-align: center;
        font-size: 24px;
        font-weight: bold;
        letter-spacing: 1px;
        box-shadow: 0 0 15px rgba(0,255,255,0.3);
    }

    /* TABS */
    .tabs {
        display: flex;
        background: #111827;
        border-bottom: 1px solid #222;
    }

    .tabs button {
        flex: 1;
        padding: 12px;
        border: none;
        background: none;
        color: white;
        font-weight: bold;
        cursor: pointer;
        transition: 0.2s;
    }

    .tabs button:hover {
        background: #1f2937;
        color: #00f5ff;
    }

    .tab-content {
        display: none;
        padding: 20px;
    }

    .active {
        display: block;
    }

    /* CARDS */
    .card {
        background: #111827;
        padding: 15px;
        border-radius: 10px;
        margin-top: 10px;
        border: 1px solid #222;
        box-shadow: 0 0 10px rgba(0,255,255,0.08);
    }

    /* BUTTONS */
    button.action {
        padding: 10px;
        border: none;
        border-radius: 8px;
        cursor: pointer;
        margin-top: 10px;
        width: 100%;
        font-weight: bold;
    }

    .green { background: #00f5ff; color: black; }
    .red { background: #ff4d6d; color: white; }

    /* ADMIN */
    #adminPanel {
        display: none;
        margin-top: 10px;
        padding: 15px;
        background: #0f172a;
        border-radius: 10px;
        border: 1px solid #222;
    }

    input {
        padding: 10px;
        width: 100%;
        margin-top: 10px;
        border-radius: 6px;
        border: none;
        background: #1f2937;
        color: white;
    }

    /* CASH APP */
    .cashapp {
        background: linear-gradient(90deg, #00f5ff, #22c55e);
        padding: 15px;
        border-radius: 10px;
        font-weight: bold;
        text-align: center;
        color: black;
    }

</style>
</head>

<body>

<header>Jesse & Co</header>

<div class="tabs">
    <button onclick="openTab('home')">Home</button>
    <button onclick="openTab('jobs')">Jobs</button>
    <button onclick="openTab('cashapp')">Cash App</button>
    <button onclick="openTab('admin')">Admin</button>
</div>

<!-- HOME -->
<div id="home" class="tab-content active">
    <div class="card">
        <h2>Welcome</h2>
        <p>This is Jesse & Co — your main hub.</p>
    </div>

    <div class="card">
        <h3>Status</h3>
        <p id="statusText">Not coming through</p>
    </div>
</div>

<!-- JOBS -->
<div id="jobs" class="tab-content">
    <div class="card">
        <h2>Jobs</h2>
        <p>More job sections will go here later.</p>
    </div>
</div>

<!-- CASH APP -->
<div id="cashapp" class="tab-content">
    <div class="cashapp">
        Cash App: $Cocoabeans12
    </div>
</div>

<!-- ADMIN -->
<div id="admin" class="tab-content">
    <div class="card">
        <h2>Admin Login</h2>
        <input type="password" id="adminPass" placeholder="Enter password">
        <button class="action green" onclick="login()">Login</button>
    </div>

    <div id="adminPanel">
        <h3>Admin Controls</h3>

        <!-- STATUS BUTTONS ONLY HERE -->
        <button class="action green" onclick="setStatus(true)">Coming Through</button>
        <button class="action red" onclick="setStatus(false)">Not Coming Through</button>

        <input id="customStatus" placeholder="Custom status">
        <button class="action green" onclick="updateStatus()">Update</button>

        <button class="action red" onclick="logout()">Logout</button>
    </div>
</div>

<script>
function openTab(tab) {
    document.querySelectorAll('.tab-content').forEach(t => t.classList.remove('active'));
    document.getElementById(tab).classList.add('active');
}

function login() {
    let pass = document.getElementById("adminPass").value;
    if (pass === "0827") {
        document.getElementById("adminPanel").style.display = "block";
        alert("Admin unlocked");
    } else {
        alert("Wrong password");
    }
}

function logout() {
    document.getElementById("adminPanel").style.display = "none";
}

function setStatus(state) {
    document.getElementById("statusText").innerText =
        state ? "Coming through" : "Not coming through";
}

function updateStatus() {
    let text = document.getElementById("customStatus").value;
    document.getElementById("statusText").innerText = text;
}
</script>

</body>
</html>